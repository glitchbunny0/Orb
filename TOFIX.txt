- [Post-process] Implement phrase repetition detection as well, not just anti-slop detection. Tell the model to write about something else entirely for this sentence.

- Improvement: Character management UI
[Done]  + Button to show a searchable modal that displays all characters under Create and Import.
[Done]  + Current characte list to show most recent ones.
[Done]  + Tag-based search.
  + Handle importing characters with the same name => List the chars with that name, ask user whether to make a new one or replace, and which one.

- Feature: Compress history and make a new conversation from there.
  + Display a modal asking how many recent chat messages to keep (max 8).
  + Model to summarize the story so far, keeping important dialogues ad-verbatim, and key beats and milestones, relationships => User reviews and approves.
  + This prompt is very important because representation for the next cycle is very important.
  + On user approval, create a new conversation where first message is the previously generated summary. Append the recent chat messages that the user wanted to keep.

-- Not yet --
- Feature: Tags and search for Convos.
- Feature: Macros.
- Feature: Add a magic button to the AI's message for "rewrite with custom direction." Clicking it opens a small input box where the user can specify the rewrite direction.
